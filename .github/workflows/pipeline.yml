name: CI/CD Pipeline

on:
  push:
    branches: [develop]
  pull_request:

jobs:
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      checks: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run actionlint
        uses: reviewdog/action-actionlint@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          reporter: github-check
          # actionlint's bundled action-input snapshot (even in v1.7.12) predates the
          # `client-id` input on actions/create-github-app-token@v3. Drop these two
          # -ignore lines once the bundled metadata catches up.
          actionlint_flags: >-
            -ignore 'input "client-id" is not defined in action "actions/create-github-app-token'
            -ignore 'missing input "app-id" which is required by action "actions/create-github-app-token'
  self-test:
    name: Run against fixture role
    uses: ./.github/workflows/ansible-role-ci.yml
    with:
      ansible_versions: '["2.19"]'
      lint_os: '["ubuntu-latest"]'
      e2e_os: '["ubuntu-latest"]'

  build-results:
    name: Build results
    if: always()
    runs-on: ubuntu-latest
    needs:
      - actionlint
      - self-test
    steps:
      - run: exit 1
        # see https://stackoverflow.com/a/67532120/4907315
        if: >-
          ${{
                contains(needs.*.result, 'failure')
            || contains(needs.*.result, 'cancelled')
                || contains(needs.*.result, 'skipped')
                }}
